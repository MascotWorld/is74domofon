# IS74 Домофон - Home Assistant Integration

[![hacs_badge](https://img.shields.io/badge/HACS-Custom-orange.svg)](https://github.com/hacs/integration)

Интеграция для управления домофоном IS74 (ИС74, Интерсвязь) через Home Assistant.

## 🏠 Возможности

- 🚪 **Управление дверью** - открытие двери из Home Assistant
- 📹 **Камеры** - просмотр видео с камер домофона
- 🔔 **Push уведомления** - получение уведомлений о входящих звонках через FCM
- 🤖 **Автооткрытие** - настройка автоматического открытия по расписанию
- 📦 **Режим курьера** - одноразовое автооткрытие для доставки
- 🌐 **Веб-панель** - быстрый доступ к веб-интерфейсу

## 📦 Установка

### Через HACS

1. Откройте HACS в Home Assistant
2. Нажмите на три точки (⋮) в правом верхнем углу
3. Выберите **Пользовательские репозитории**
4. В поле "Репозиторий" введите: `https://github.com/your-username/is74domofon`
5. В поле "Категория" выберите: **Интеграция**
6. Нажмите **Добавить**
7. Закройте окно и найдите "IS74 Домофон" в списке
8. Нажмите на неё и выберите **Скачать**
9. Перезапустите Home Assistant

### Ручная установка (альтернатива)

1. Скопируйте папку `custom_components/is74_domofon` в `config/custom_components/` вашего Home Assistant
2. Перезапустите Home Assistant

### Установка карточки (вручную)

Карточку проще установить вручную:

1. Скопируйте файл `is74-domofon-card/is74-domofon-card.js` в папку `config/www/` вашего Home Assistant
2. Перейдите в **Настройки → Панели управления → три точки (⋮) → Ресурсы**
3. Нажмите **Добавить ресурс**
4. Введите URL: `/local/is74-domofon-card.js`
5. Тип: **JavaScript модуль**
6. Нажмите **Создать**

Или добавьте в `configuration.yaml`:

```yaml
lovelace:
  resources:
    - url: /local/is74-domofon-card.js
      type: module
```

## ⚙️ Настройка

### Вариант 1: Встроенный сервер (рекомендуется) ⭐

Интеграция имеет **встроенный API сервер** — никаких дополнительных контейнеров не нужно!

1. Установите интеграцию через HACS или вручную
2. Перейдите в **Настройки → Устройства и службы → Добавить интеграцию**
3. Найдите "IS74 Домофон"
4. Включите "Использовать встроенный сервер" (по умолчанию включено)
5. Нажмите "Подтвердить"
6. Откройте `http://homeassistant.local:8099` для авторизации

После авторизации перезагрузите интеграцию и все устройства появятся автоматически.

---

### Вариант 2: Add-on (для Home Assistant OS/Supervised)

1. Скопируйте папку `is74-domofon-addon` в `/addons/` на Home Assistant:
   ```bash
   scp -r is74-domofon-addon root@homeassistant.local:/addons/is74_domofon
   ```

2. Перейдите в **Настройки → Дополнения → Магазин дополнений**

3. Нажмите **⋮ → Проверить наличие обновлений**

4. Найдите "IS74 Domofon" в разделе "Локальные дополнения" и установите

5. Запустите add-on и откройте веб-интерфейс для авторизации

### Вариант 2: Docker (для Home Assistant Container)

1. Склонируйте репозиторий рядом с вашим HA:
```bash
git clone https://github.com/your-username/is74domofon
cd is74domofon
```

2. Запустите через Docker Compose:
```bash
docker-compose up -d
```

3. Откройте веб-интерфейс http://localhost:8000 и пройдите авторизацию

**Важно:** Если ваш Home Assistant в своей Docker сети, добавьте контейнер в ту же сеть. Отредактируйте `docker-compose.yaml`:

```yaml
networks:
  homeassistant:
    external: true
    name: ИМЯ_ВАШЕЙ_СЕТИ  # узнать: docker network ls
```

Или используйте `network_mode: host`:
```yaml
services:
  is74-domofon:
    network_mode: host
```

### Вариант 3: Ручной запуск (без Docker)

```bash
cd is74domofon
pip install -r requirements.txt
python run_api.py
```

### Добавление интеграции

1. Перейдите в Настройки → Устройства и службы
2. Нажмите "Добавить интеграцию"
3. Найдите "IS74 Домофон"
4. Введите URL вашего API сервера (по умолчанию: `http://localhost:8000`)

## 🎨 Lovelace карточка

### Базовая конфигурация

```yaml
type: custom:is74-domofon-card
title: Мой домофон
api_url: http://192.168.1.100:8000
entities:
  device_sensor: sensor.is74_domofon_status
  service_sensor: sensor.is74_service_status
  fcm_sensor: sensor.is74_fcm_push
  open_button: button.is74_open_door
  reject_button: button.is74_reject_call
  auto_open_switch: switch.is74_auto_open
  courier_switch: switch.is74_courier
  fcm_switch: switch.is74_fcm
  cameras:
    - camera.is74_camera_entrance
```

### Альтернативная простая карточка (YAML)

Если вы предпочитаете стандартные карточки:

```yaml
type: vertical-stack
cards:
  - type: entities
    entities:
      - entity: sensor.is74_domofon_status
        name: Домофон
        secondary_info: last-changed
      - entity: switch.is74_auto_open
        name: Автооткрытие
  - type: horizontal-stack
    cards:
      - type: tile
        entity: button.is74_open_door
        name: Открыть
        color: green
        tap_action:
          action: call-service
          service: button.press
          target:
            entity_id: button.is74_open_door
      - type: tile
        entity: switch.is74_courier
        name: Курьер
        color: blue
      - type: tile
        entity: button.is74_reject_call
        name: Сброс
        color: red
```

## 🔧 Сервисы

Интеграция предоставляет следующие сервисы:

| Сервис | Описание |
|--------|----------|
| `is74_domofon.open_door` | Открыть дверь |
| `is74_domofon.toggle_auto_open` | Вкл/выкл автооткрытие |
| `is74_domofon.start_fcm` | Запустить Push сервис |
| `is74_domofon.stop_fcm` | Остановить Push сервис |

### Пример автоматизации

```yaml
automation:
  - alias: "Автооткрытие в рабочие дни"
    trigger:
      platform: time
      at: "09:00:00"
    condition:
      condition: time
      weekday:
        - mon
        - tue
        - wed
        - thu
        - fri
    action:
      service: is74_domofon.toggle_auto_open
      data:
        enabled: true

  - alias: "Выключение автооткрытия вечером"
    trigger:
      platform: time
      at: "18:00:00"
    action:
      service: is74_domofon.toggle_auto_open
      data:
        enabled: false
```

## 📱 Уведомления

Для получения уведомлений о звонках настройте автоматизацию:

```yaml
automation:
  - alias: "Уведомление о звонке в домофон"
    trigger:
      platform: event
      event_type: is74_domofon_incoming_call
    action:
      service: notify.mobile_app_your_phone
      data:
        title: "🚪 Звонок в домофон"
        message: "Кто-то звонит в дверь"
        data:
          actions:
            - action: OPEN_DOOR
              title: "Открыть"
            - action: REJECT_CALL
              title: "Отклонить"
```

## 🐛 Устранение неполадок

### Интеграция не находит сервер

1. Проверьте, что IS74 Integration Service запущен
2. Убедитесь, что URL доступен из Home Assistant
3. Проверьте логи: `tail -f ~/.homeassistant/home-assistant.log | grep is74`

### Камеры не показывают изображение

1. Проверьте авторизацию на сервере IS74
2. Убедитесь, что камеры доступны в API (`GET /cameras`)
3. Проверьте сетевую доступность CDN камер

### Push уведомления не приходят

1. Инициализируйте FCM: `POST /fcm/init`
2. Запустите Push сервис: `POST /fcm/start`
3. Проверьте статус: `GET /fcm/status`

## 📄 Лицензия

MIT License

## 🤝 Вклад

Pull requests приветствуются! Для крупных изменений сначала откройте issue.

