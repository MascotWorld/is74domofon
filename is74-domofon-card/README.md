# IS74 Domofon Card

Кастомная Lovelace карточка для управления домофоном IS74 в Home Assistant.

![Card Preview](preview.png)

## Установка

### Через HACS

1. Откройте HACS → Frontend
2. Добавьте этот репозиторий как пользовательский
3. Установите "IS74 Domofon Card"
4. Перезагрузите браузер

### Вручную

1. Скопируйте `is74-domofon-card.js` в `config/www/`
2. Добавьте ресурс в Lovelace:

```yaml
resources:
  - url: /local/is74-domofon-card.js
    type: module
```

## Конфигурация

```yaml
type: custom:is74-domofon-card
title: Мой домофон
api_url: http://192.168.1.100:8000
entities:
  device_sensor: sensor.is74_domofon_status
  service_sensor: sensor.is74_service_status  
  fcm_sensor: sensor.is74_fcm_push
  open_button: button.is74_open_door
  reject_button: button.is74_reject_call
  auto_open_switch: switch.is74_auto_open
  courier_switch: switch.is74_courier
  fcm_switch: switch.is74_fcm
  cameras:
    - camera.is74_entrance
```

## Параметры

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `title` | string | нет | Заголовок карточки |
| `api_url` | string | нет | URL веб-панели |
| `entities` | object | да | Сущности интеграции |

### Entities

| Ключ | Описание |
|------|----------|
| `device_sensor` | Сенсор статуса устройства |
| `service_sensor` | Сенсор статуса сервиса |
| `fcm_sensor` | Сенсор статуса FCM |
| `open_button` | Кнопка открытия двери |
| `reject_button` | Кнопка сброса звонка |
| `auto_open_switch` | Переключатель автооткрытия |
| `courier_switch` | Переключатель режима курьера |
| `fcm_switch` | Переключатель FCM |
| `cameras` | Список camera entities |

## Особенности

- 🎨 Современный dark дизайн
- 📱 Адаптивная верстка
- 🔄 Три вкладки: Управление, Камеры, Настройки
- 🚪 Кнопки быстрого управления
- 📹 Превью камер
- 🌐 Быстрый доступ к веб-панели

